# Used only by `vite dev` to know where the Go backend lives.
# In a production build, the SPA uses relative paths (/api/..., /media/...)
# and is served by the same Go binary, so this is not needed.
VITE_API_URL=http://localhost:8080
